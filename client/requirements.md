## Packages
framer-motion | Smooth transitions and dialog animations
clsx | Utility for conditional classes (standard with shadcn)
tailwind-merge | Utility for merging tailwind classes (standard with shadcn)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
